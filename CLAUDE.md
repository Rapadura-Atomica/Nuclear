# CLAUDE.md

> The canonical, cross-machine project context for the **Nuclear** fork lives in
> [`tools/nuclear_claude/`](tools/nuclear_claude/) so it stays version-controlled and
> synced across machines/sessions. This root file only imports it, so the full context
> auto-loads in every session.
>
> Edit the context in `tools/nuclear_claude/CLAUDE.md`, not here.

@tools/nuclear_claude/CLAUDE.md
